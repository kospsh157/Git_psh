// 마찬가지로 IEEE754 DOUBLE 이란 보장이 없다. 

// 그저 float보다 크기가 이상이면 됨.
// float은 그저 double 보다 빠르게 연상하기 위해 나온 작은 부동소수점

// double 리터럴? >> 없다. 그냥 double pi1 = 3.14; 이렇게 선언해도 double인지 알아먹는다. 

// unsigned 형은 없다. 따라서 signed형도 없다. (대체로 부동소수점은 unsigned/signed형이 없음)

// 표준에 상관없이 현재 안전하게 생각해도 되는 것 
// 1. 크기 : 64비트
// 2. 범위 : IEEE754 Double 과 동일 (단, 소형기기 갈때마는 조심하자)

