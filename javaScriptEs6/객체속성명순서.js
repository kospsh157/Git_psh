// 자바스크립트에서 객체의 속성들을 나열할 때 순서가 있다.
/*  
    1. 먼저 숫자키가 먼저 오고 숫자 오름차순으로 나열된다.
    2. 문자열키나 심볼키들은 객체에 정의된 순서를 그대로 따른다.
    3. 그러나 예외가 있는데, 
        1. Object.keys();
        2. For - in  루프문
        3. JSON.stringify()
        위 3가지들은 불특정한 나열 순서를 가진다. 
*/

