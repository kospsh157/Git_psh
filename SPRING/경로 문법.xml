<!-- 
    classpath:
    는 대개 
    src/main/java
    src/main/resource  이 두 곳을 가리킨다.
    
 -->

 <context-param> 
    <param-name>contextConfigLocation</param-name> 
    <param-value>classpath:/**/applicationContext.xml</param-value> 
</context-param>

<!-- 
    
    위에서 ** 는 모든 층의 하위 폴더를 다 포함한다. 
    반면 * 는 레벨1층의 모든 하위 폴더를 포함한다. 즉 하위 폴더의 한칸만 모두 포함한다는 뜻이다. 

 -->