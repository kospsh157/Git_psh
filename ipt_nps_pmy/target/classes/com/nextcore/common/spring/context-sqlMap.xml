<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:mybatis="http://mybatis.org/schema/mybatis-spring"
    xsi:schemaLocation="
        http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        ">

    <!-- SqlSession config start -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="typeAliasesPackage" value="com.nextcore" /> <!-- wildcards(*)를 이용한 패턴매칭 사용불가. 상위 패키지 등록 // FIXME 추후 package common에 상태 보고 수정하겠음. - by 재락 -->
        <property name="configLocation" value="#{mybatis['sqlmap.configLocation']}" />
        <property name="mapperLocations">
            <array>
                <value>#{mybatis['sqlmap.mapperLocation']}</value>
            </array>
        </property>
    </bean>

    <bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate" destroy-method="clearCache">
        <constructor-arg ref="sqlSessionFactory" />
    </bean>

    <!-- mybatis mapper scan -->
<!--     <mybatis:scan base-package="kr.co.credif.**.**.mapper, kr.co.credif.lams.**.**.mapper, kr.co.credif.lams.**.**.**.mapper" template-ref="sqlSession" /> -->
    <mybatis:scan base-package="com.nextcore.portal.**.mapper" template-ref="sqlSession" />
    <!-- SqlSession config end -->


</beans>
