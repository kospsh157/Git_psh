function setInfo(name, old){    
    this._name = name;
    this._info = old; 
}
// 위와 같은 함수가 있을 때 setInfo(name, old)에서 가로 안에 들어가있는 name과 old가 바로 매개변수입니다. 

setInfo('John', 30);
// 위와 같이 매개변수의 형식에 맞춰서, 실제 값이 전달될 때 이 실제 데이터 "John", 30 를 전달인자, 인자값, 혹은 argument라고 합니다.



